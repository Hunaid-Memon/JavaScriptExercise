let usernames = ['Admin', 'Sajid', 'Sultan', 'Rashid', 'Majid'];

usernames.splice(0,usernames.length)

if(usernames.length === 0) {
    console.log('list is empty, We need to find some users! ' + usernames.length);
}

